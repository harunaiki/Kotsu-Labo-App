# 骨ラボ 予約・カルテ管理システム — Google スプレッドシート版 セットアップ手順書

配布用の単一HTMLファイル（`kotsu-lab-sheets.html`。React・lucide-react を内包、CDN不要）はそのままに、データの保存先を **Google スプレッドシート** に切り替えるための手順です。
構成は「HTML アプリ（画面）＋ Apps Script（API）＋ スプレッドシート（DB）」の3層です。

> 補足: `kotsu-lab-sheets.jsx` はビルド前のソースで、配布物はそれをビルドした単一HTMLです。GitHub Pages では `index.html`（リポジトリ `harunaiki/Kotsu-Labo-App` の `html_version` ブランチ、`https://harunaiki.github.io/Kotsu-Labo-App/`）として配信します。実際に配信・設定するのは **HTML ファイル** です（ソースから再ビルドする場合は §5 の補足を参照）。
>
> **v2.2 の追加**: QR/直リンク（ハッシュルーティング `#/reserve`・`#/mypage`）と、お客様のパスワード認証（クライアント側・`passHash` 列）に対応しました。詳細は本書「QRコード・直リンクとパスワード認証」節を参照してください。

---

## 全体像

```
[ブラウザ: kotsu-lab-sheets.html]
        │  fetch (GET=読込 / POST=保存)
        ▼
[Apps Script Web アプリ: kotsu-lab-sheets-backend.gs]   ← /exec URL
        │  読み書き
        ▼
[Google スプレッドシート（5タブ = 5テーブル）]
   companies / customers / practitioners / sessions / reservations
```

各テーブルは1タブに対応。各行は `_json` 列にレコード全体（正本）を保存し、隣に可読用の列も並びます。

---

## 手順

### 1. スプレッドシートを作成
1. Google ドライブで新規スプレッドシートを作成（名前は任意。例: `骨ラボDB`）。

### 2. Apps Script を貼り付け
1. スプレッドシートのメニュー **拡張機能 → Apps Script** を開く。
2. 既定の `Code.gs` の中身をすべて削除し、`kotsu-lab-sheets-backend.gs` の内容を貼り付けて保存（💾）。

### 3. タブ（テーブル）を用意
1. スクリプトエディタ上部の関数選択で **`setupSheets`** を選び **▶ 実行**。
2. 初回は権限承認を求められます → 自分の Google アカウントで許可（「詳細」→「（安全でないページ）に移動」→ 許可）。
3. スプレッドシートに `companies / customers / practitioners / sessions / reservations` の5タブが作られます。

### 4. Web アプリとしてデプロイ
1. スクリプトエディタ右上 **デプロイ → 新しいデプロイ**。
2. 種類（⚙）で **ウェブアプリ** を選択。
3. 設定：
   - **次のユーザーとして実行**: 自分
   - **アクセスできるユーザー**: **全員**（ログイン不要で読み書きさせる場合）
4. **デプロイ** → 表示される **ウェブアプリ URL（`https://script.google.com/macros/s/XXXX/exec`）をコピー**。

> ⚠️ コードを更新したら、**デプロイ → デプロイを管理 → 編集（鉛筆）→ バージョン「新規」→ デプロイ** で再デプロイしてください。URL は変わりません。

### 5. フロント（HTML）に URL を設定

1. `kotsu-lab-sheets.html` をテキストエディタで開き、ファイル冒頭付近の次の行を編集します（`<script>` ブロック内）：
   ```html
   window.SHEETS_API_URL = ''; // ← ここに /exec URL を貼る
   ```
   例：
   ```html
   window.SHEETS_API_URL = 'https://script.google.com/macros/s/AKfy.../exec';
   ```
2. 保存して、HTML をブラウザで開く（または配信先へアップロード）。初回はシード（サンプルデータ）がスプレッドシートに書き込まれます。

これで予約・カルテの追加・編集がスプレッドシートに保存されるようになります。

> **補足（ソースから再ビルドする場合）**: `kotsu-lab-sheets.jsx` から作り直すときは、esbuild で React・lucide-react・`js-sha256` を内包した単一HTMLにバンドルします。JSX 側の `SHEETS_API_URL` は `window.SHEETS_API_URL` を読む実装になっているため、接続先 URL は **HTML 冒頭の `window.SHEETS_API_URL` で設定**すれば足り、JSX の再編集は不要です（既存デプロイの URL もそのまま引き継がれます）。
>
> **補足（配信先）**: HTML は `window.storage` が無い通常ブラウザでは `localStorage` を保存先に使うフォールバックを内蔵しているため、Netlify 等の静的ホスティングやローカルのファイル/サーバーからそのまま開けます。

---

## 動作確認

- **API 単体**: ブラウザで `（あなたのURL）?action=ping` を開き、`{"ok":true,"pong":true,...}` が返ればOK。
- **スクリプト内**: エディタで関数 `selfTest` を実行 → 実行ログにレコードが出ればOK。
- **アプリ**: デモ資格情報でログインし、予約を1件追加 → スプレッドシートの `reservations` タブに行が増えることを確認。

デモ資格情報
- 個人のお客様: お客様コード `C-8F3KQ` ＋ パスワード `1234`
- 法人のお客様: 企業コード `YMD-4471` ＋ 社員番号 `1024` ＋ パスワード `1234`
- 企業ご担当者: `YMD-4471` / `yamada`
- 管理者: ID `admin` / パスワード `kotsu`

（シードで作成されるお客様の初期パスワードはすべて `1234` です。本番運用前に管理者ページから変更してください。）

---

## QRコード・直リンクとパスワード認証（v2.2）

### 直リンク（ハッシュルーティング）
URL のハッシュで、特定の画面を直接開けます。サーバ設定は不要で、GitHub Pages のような静的ホスティングでそのまま動きます。

| URL | 開く画面 |
| --- | --- |
| `https://harunaiki.github.io/Kotsu-Labo-App/#/reserve` | お客様 予約フォーム（ログイン不要） |
| `https://harunaiki.github.io/Kotsu-Labo-App/#/mypage` | お客様マイページのログイン |
| `https://harunaiki.github.io/Kotsu-Labo-App/`（ハッシュ無し） | トップ（ランディング） |

- 配布用 QR は予約フォーム（`#/reserve`）に直接遷移します。トップ画面のQR・リンク、および同梱の `reserve-qr.png`（チラシ・LINE 配布用）はこのURLを指します。
- **配信URLが変わる場合**（独自ドメイン等）は、ソースの `RESERVE_URL` を新URLに変更して QR を作り直してください（`#/reserve` を含めること）。

### パスワード認証（お客様）
- **新規登録時**にお客様自身がパスワードを設定します（4文字以上）。マイページのログインと、次回以降の予約フォームでの本人確認に使用します。
- パスワードは平文では保存せず、`customers` タブの **`passHash`**（SHA-256＋ソルト）として保持します。
- **パスワードの設定・再設定**は管理者ページのカルテ詳細「マイページのパスワード」から行えます（失念対応・既存顧客の移行）。
- ⚠️ **セキュリティ上の重要事項**: この認証はブラウザ側（クライアント）での照合です。Apps Script の `/exec` は無認証で全件を返す公開APIのため、パスワード照合は実質的な保護になりません（ブラウザに全データが渡ります）。**要配慮個人情報（カルテ）を本番で扱う場合は、サーバ側でのパスワード検証＋本人分のみ返却する構成への移行が必須**です（要件定義書 10章）。

---

## 仕様メモ・注意点

- **保存方式**: テーブル単位の「全置換」。アプリが該当タブを丸ごと書き直します。
  `_json` 列が正本なので、可読列を手で編集しても読み込みには反映されません（編集は `_json` 側、または手編集後にアプリ側で再保存）。
- **同時編集**: `LockService` で書き込みを直列化していますが、全置換方式のため、複数人がほぼ同時に別レコードを更新すると後勝ちになり得ます。少人数運用が前提です。
- **CORS / 通信**: フロントは POST を `Content-Type: text/plain` で送り、プリフライトを回避しています。
  claude.ai のプレビュー画面内など、外部ドメインへの通信が制限される環境では Sheets に届かないことがあります。その場合アプリは自動でブラウザ内ストレージ（端末ローカル）にフォールバックし、画面は問題なく動作します。**実運用では、このアプリを自前のホスティング（Netlify 等）や手元の開発サーバーで動かしてください。**
- **`window.SHEETS_API_URL` が空欄のとき**: 従来どおりブラウザ内ストレージ（`window.storage` または `localStorage`）で動作します（デモ・体験用）。
- **ローカル保存の名前空間（v2.2 で更新）**: ブラウザ内ストレージのキーは `kotsulab5:*` に変わりました（旧 `kotsulab4`）。ローカル保存で使っていた端末は、初回アクセス時に新しいシードで再初期化されます。**スプレッドシート側のタブ名は変わりません**（名前空間はローカルキーのみに作用）。
- **既存スプレッドシート顧客の移行（重要）**: Sheets に既存のお客様データがある場合、その行には `passHash` が無いため、そのままではログインできません。**管理者ページ → カルテ → 対象のお客様 → 「マイページのパスワード」** からパスワードを設定してください（`passHash` 列が自動で書き込まれます）。シードからの新規環境では不要です。
- **バックアップ**: スプレッドシートをコピーするだけで、その時点のデータを丸ごと保全できます（手順は次節）。

---

## バックアップとロールバック（データ復元）

### 仕組み（重要）
読み込みは各タブの **`_json` 列（B列）を正本**とします。アプリはこの列を `JSON.parse` してレコードを復元し、`_json` が読める限り**可読列（氏名・コード等の隣の列）は参照しません**（`_json` が無い／壊れているときだけ可読列から復元する保険動作）。
保存は、対象タブを**いったんクリアしてから全体を書き直す「全置換」方式**です。

このため、**可読列だけを手で書き換えてもロールバックにはなりません**。復元するときは、ヘッダ行と `_json` 列を含めて**行ごとまるごと**戻す必要があります。

### バックアップの取り方
- **推奨**: 連携先スプレッドシートを **「ファイル → コピーを作成」** で複製し、日付入りの名前で保管する（例: `骨ラボDB_backup_2026-06-29`）。`_json` 列も含めて全列が確実に保全されます。
- index.html の差し替えなど、データに影響し得る作業の**前**に取得しておくと安心です。

### ロールバックの手順
1. **アプリを誰も開いていない状態にする**（全員ログアウト／タブを閉じる）。全置換方式のため、古い画面から保存が走ると復元内容が上書きされます。
2. バックアップ（コピーしたスプレッドシート）から、**5タブすべて**を同一時点のものとして戻す。`companies / customers / practitioners / sessions / reservations` は互いに ID で参照し合うため（companyId・sessionId・customerId 等）、**タブごとに別時点を混在させない**こと。
   - 方法A（簡単・確実）: **連携先をバックアップ側のスプレッドシートに一時的に差し替える**。Apps Script のスプレッドシートID（または `SHEETS_API_URL` の指す先）をバックアップのものに向ける。
   - 方法B: 現行スプレッドシートの各タブを、バックアップ側の同名タブの**全範囲（ヘッダ＋全列、`_json` 列を含む）**で上書きする。タブ単位・列単位でまるごとコピーする（セル単位の手入力は避ける）。
3. 復元後、利用者に**リロード**してもらう。アプリは起動時にタブを読み直し、データが入っていればシード（サンプル）での再初期化は起きません。

### 注意点
- **`_json` 列を必ず含めて戻す**。可読列だけの上書きは反映されません。
- 手作業のコピペで `_json` 内の長い JSON が改行・セル分割で崩れると、その行だけ可読列フォールバックに落ち、一部フィールドが欠けることがあります。**タブ／列まるごとのコピー**か、**スプレッドシートのコピーごと差し替え**でこのリスクを避けられます。
- 5タブを揃えて戻すことで、予約⇄お客様⇄施術日の参照整合性が保たれます。

---

## トラブルシュート

| 症状 | 確認すること |
| --- | --- |
| 保存されない／毎回シードに戻る | HTML の `window.SHEETS_API_URL` が `/exec` URL になっているか。`?action=ping` が `ok:true` を返すか。 |
| `unknown table` エラー | タブ名が5種のいずれかと完全一致しているか（`setupSheets` で再生成）。 |
| 権限エラー | デプロイの「アクセスできるユーザー」が **全員** か。コード変更後に再デプロイしたか。 |
| プレビューで Sheets に保存されない | claude.ai プレビューの通信制限。自前ホスティング／ローカルで実行する。 |
| 文字化け・配列が崩れる | 可読列ではなく `_json` 列を確認（こちらが正本）。 |
| お客様がログインできない（パスワードが違う） | 既存Sheets顧客は `passHash` 未設定の可能性。管理者ページ → カルテ → 「マイページのパスワード」で設定する。 |
| QR/リンクを開いてもトップが出る | URL に `#/reserve` 等のハッシュが含まれているか。独自ドメインへ移設した場合は `RESERVE_URL` とQRを作り直す。 |
| ローカルで毎回データが初期化される | v2.2 で名前空間が `kotsulab5` に更新。Sheets未接続だと端末ローカル保存のため共有されない（複数人運用は Sheets 連携が前提）。 |
| 復元（ロールバック）したのに古いデータに戻る | 復元中にアプリが開いていて、全置換保存で上書きされた可能性。全員を閉じた状態で復元し、終わってからリロードする。 |
| 復元したのに一部の項目が空になる | 可読列だけ戻していないか。`_json` 列を含めて行ごと戻す。手コピペで `_json` が崩れていないかも確認（タブ／列まるごとコピー推奨）。 |
